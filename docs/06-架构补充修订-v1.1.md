# 架构补充修订 v1.1

| 项目 | 内容 |
|---|---|
| 基准 | TexasHoldem_Architecture v1.0 |
| 修订版本 | v1.1 |
| 状态 | ✅ 已定稿（2026-07-05 评审会） |

---

## 修订摘要

| 类别 | 变更 |
|---|---|
| 修正 | 架构图 LB→MySQL、持久化策略、筹码类型 |
| 补充 | 服务拆分、Room 路由、Bot 集成、可观测性、故障恢复 |
| 新增 | 完整 API 列表、错误码、CI 测试要求 |

---

## 一、架构图修正

**删除** Load Balancer 到 MySQL 的直连。正确路径：

```
Client → LB → API Server  → MySQL / Redis
Client → LB → Room Server → Redis (+ MySQL 异步结算)
              Room Server → Bot Engine (gRPC, v1.5+ 预留)
              Room Server → RuleBot (v1.0 进程内模块)
```

---

## 二、服务拆分建议

| 服务 | 职责 | 状态 | 技术 |
|---|---|---|---|
| `api-gateway` | HTTPS 路由、JWT 校验、限流 | 无状态 | Nginx/HAProxy |
| `user-service` | 登录、资料、好友(v1.5) | 无状态 | NestJS |
| `shop-service` | 充值、订单回调 | 无状态 | NestJS |
| `match-service` | 快速开始、官方场分配 | 无状态 | NestJS |
| `room-server` | WebSocket、状态机、结算、**v1.0 规则 Bot** | **有状态** | NestJS + Socket.io + XState |
| `bot-engine` | GTO 决策 | 无状态 | Python gRPC（**v1.5+**） |
| `admin-service` | 后台 CMS | 无状态 | NestJS |
| `risk-worker` | 筹码转移检测、定时对账 | 无状态 | Node worker |

> v1.0 部署单元：**api-service** + **room-server**（含规则 Bot 模块）。无需独立 bot-engine Pod。

---

## 三、Room Server 有状态路由

### 3.1 路由规则

1. 创建房间时，Match Service 选择负载最低 Room Pod
2. 写入 Redis：`room:pod:{room_id}` = `pod-ip:port`
3. Gateway 根据 `room_id` 一致性哈希 或 查 Redis 转发 WS

### 3.2 故障转移（v1.0 最小方案）

- Room Pod 崩溃 → K8s 重启 Pod
- 从 Redis `room:state:{room_id}` 恢复（AOF 持久化）
- 若 Redis 数据也在 → 本手牌作废，退还 antes/blinds 至各玩家 Redis 筹码，告警人工介入
- v1.1+ 引入 Room 主从热备

---

## 四、数据层修订

### 4.1 筹码类型

- `users.chips_balance`：**BIGINT UNSIGNED**（替换 DECIMAL）
- 所有金额字段统一 BIGINT

### 4.2 持久化策略（与 PRD v1.5 对齐）

```
对局中     → Redis only
END_HAND   → hand_histories + rake 流水（异步队列）
CASH_OUT   → MySQL 事务（chip_transactions + users）
```

### 4.3 完整 Schema

见 `03-数据库设计完整版.md`

---

## 五、Bot 集成

### 5.1 v1.0 规则型 Bot（Room Server 内置）

```typescript
// packages/poker-engine/src/bot/rule-bot.ts
interface BotDecisionInput {
  holeCards: Card[];
  communityCards: Card[];
  potSize: number;
  toCall: number;
  stack: number;
  position: 'BTN' | 'SB' | 'BB' | 'UTG' | ...;
  actionHistory: ActionRecord[];
}
```

**策略要点**：
- Preflop：位置 + 手牌强度表（如 77+, AJo+, KQs）
- Postflop：底池赔率 + 简化牌力估算
- 3–5% 随机非最优决策（拟人失误）
- 拟人延迟：返回 `thinkTimeMs` 1,000–5,000（复杂局 3,000–8,000）
- SLA：同步调用 P99 **< 50ms**

### 5.2 v1.5+ GTO 引擎（gRPC，预留）

```protobuf
service BotStrategy {
  rpc Decide(BotDecisionRequest) returns (BotDecisionResponse);
}
```

- P99 延迟 < **800ms**；超时 fallback Check/Fold
- 每日预算：Redis `bot:budget:daily:{date}` 累加，超限降级

---

## 六、REST API 补充

### 6.1 统一响应格式

```json
{
  "code": 0,
  "message": "ok",
  "data": { },
  "requestId": "uuid"
}
```

### 6.2 核心test API 清单（v1.0）

| Method | Path | 说明 |
|---|---|---|
| POST | `/api/v1/auth/guest` | 游客登录 |
| POST | `/api/v1/auth/oauth` | Apple/Google 登录 |
| POST | `/api/v1/auth/refresh` | 刷新 Token |
| GET | `/api/v1/user/profile` | 用户资料 |
| PATCH | `/api/v1/user/profile` | 更新昵称/头像 |
| POST | `/api/v1/shop/mock-recharge` | **v1.0 模拟充值**（`PAYMENT_MODE=mock`） |
| POST | `/api/v1/shop/recharge` | 真实充值订单（v1.1+） |
| POST | `/api/v1/shop/callback/:channel` | 支付回调（v1.1+） |
| POST | `/api/v1/match/quick-start` | 快速开始（返回 roomId） |
| GET | `/api/v1/rooms/:id` | 房间信息 |
| POST | `/api/v1/private-room/permission` | 购买开房权限（100 筹码） |
| POST | `/api/v1/private-room/create` | 创建私人场 |

---

## 七、安全与鉴权

| 项 | 方案 |
|---|---|
| JWT Access | 2h |
| JWT Refresh | 30d，存 `user_sessions` |
| WS 连接 | `io({ auth: { token } })` |
| 底牌 | 按 socket 房间隔离下发；Redis 存明文，传输层 TLS |
| IP/设备互斥 | join_room 中间件 |
| Rate Limit | API 100/min/user；WS 30 actions/min/user |

---

## 八、可观测性

| 组件 | 方案 |
|---|---|
| 日志 | 结构化 JSON，字段：trace_id, user_id, room_id, hand_id |
| 追踪 | OpenTelemetry，`trace_id = hand_id` |
| 指标 | 在线人数、活跃桌数、P99 WS 延迟、Bot 调用延迟、rake 总量 |
| 告警 | Bot 预算耗尽、Redis 内存 >80%、Room Pod OOM、充值回调失败 |

---

## 九、CI/CD 必测用例

### 9.1 单元测试（阻塞合并）

- [ ] 边池：2人 all-in、3人不同筹码 all-in、主池+2边池
- [ ] 抽水：preflop fold → rake=0；flop 后 → floor(pot×rate)
- [ ] 奇数筹码 chop 分配
- [ ] 动态 buy-in cap 阶梯
- [ ] 带入幂等（重复 requestId）

### 9.2 集成测试

- [ ] 完整一手牌 WS 流程
- [ ] 断线重连 snapshot 一致
- [ ] IP 互斥拒绝

### 9.3 压测（Phase 4）

- 单 Pod 200 桌 / 1200 连接
- P99 消息延迟 < 100ms

---

## 十、部署拓扑（修订）

```
                    ┌─────────┐
                    │   CDN   │
                    └────┬────┘
                         │
                    ┌────▼────┐
                    │   LB    │ (WSS sticky by room_id)
                    └────┬────┘
           ┌─────────────┼─────────────┐
           │             │             │
      ┌────▼────┐   ┌────▼────┐   ┌────▼────┐
      │ API Pods│   │Room Pods│   │Bot Pods │
      └────┬────┘   └────┬────┘   └─────────┘
           │             │
           └──────┬──────┘
                  │
         ┌────────▼────────┐
         │  Redis Cluster  │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │ MySQL Primary   │
         │   + Replicas    │
         └─────────────────┘
```

---

## CHANGELOG

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-07-05 | 初版架构 |
| v1.1-draft | 2026-07-05 | 评审补充 |
| v1.1 | 2026-07-05 | 定稿：9 人桌、模拟支付、规则 Bot、私人场 v1.0 |

---

*完整 WS 协议见 `04-WebSocket协议规范.md`。*
